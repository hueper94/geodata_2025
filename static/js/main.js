// Importiere die Services
import { wfsService } from './wfs_service.js';
import { aiCleanerService } from './ai_cleaner_service.js';

// Warte auf DOM-Laden
document.addEventListener('DOMContentLoaded', () => {
    console.log('Initialisiere WFS Explorer...');
    
    // Die Services initialisieren sich selbst beim Import
    // Wir können hier zusätzliche Initialisierungen vornehmen, wenn nötig
}); 